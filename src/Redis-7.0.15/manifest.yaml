# Redis 7.0.15 Patch Overlay
# =========================
#
# 上游: https://github.com/redis/redis @ f35f36a
#
# Feature:
#   kunpeng-hw-accel/  — 鲲鹏 ARM 硬件加速（io_uring + DTOE DMA 网络路径）
#   jemalloc-arm64/    — jemalloc ARM64 pointer-tag + GC decay 策略优化
#   rdb-aof-fallback/  — RDB 损坏时降级到 AOF，避免硬停服
#
# 构建依赖:
#   rpm_build/ — RPM 打包 spec + Kraio SDK 头文件（kraio.h / libkraio.so）
#
# 安装（全量）:
#   bash tools/apply_patch.sh https://github.com/redis/redis \
#       f35f36a265403c07b119830aa4bb3b7d71653ec9 \
#       src/Redis-7.0.15 /tmp/build
#
# 子集:
#   ACTIVE_FEATURES="rdb-aof-fallback" bash tools/apply_patch.sh ...

repo: https://github.com/redis/redis
version: 7.0.15
commit: f35f36a265403c07b119830aa4bb3b7d71653ec9
