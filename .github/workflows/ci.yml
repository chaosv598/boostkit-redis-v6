# Patch overlay CI — v6.0
#
# 流程:
#   1. tools/verify.sh   — 仓根禁放 + manifest schema + clean apply
#   2. .github/lint.py manifest — manifest + features + DEP-3
#   3. .github/lint.py headers  — patch 头 schema

name: ci

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    name: ci
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pyyaml
        run: pip install pyyaml --quiet

      - name: Verify (structure + clean apply)
        run: bash tools/verify.sh

      - name: Manifest + DEP-3 lint
        run: python3 .github/lint.py manifest src/*/

      - name: Patch header lint
        run: python3 .github/lint.py headers src/*/
